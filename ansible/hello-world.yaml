- name: Say Hello Playbook
  hosts: localhost
  connection: local
  tasks:
    - name: Print a greeting
      ansible.builtin.debug:
        msg: "The user said: {{ allowed | default('Hello, World!') }}"
